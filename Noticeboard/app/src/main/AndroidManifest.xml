<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skyparking.admin.cheran">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".AttendanceReport" />
        <activity android:name=".NEWS" />
        <activity android:name=".Mainpage" />
        <activity android:name=".SelectDate" />
        <activity android:name=".fooddate" />
        <activity android:name=".Log" />
        <activity android:name=".RankCard" />

        <service android:name=".fcmFirebaseMessagingService">
            -
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".FirebaseInstanceIdService">
            -
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".food" />
        <activity android:name=".Home_work" />
        <activity android:name=".Login" />
        <activity android:name=".NoticeBoard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Select_subject" />
        <activity android:name=".Selectdateclasstest" />
        <activity android:name=".Rankcardgi" />
        <activity android:name=".activity_rankcardgii" />
        <activity android:name=".Rankcardgii" />
        <activity android:name=".Rankcardgiii" />
        <activity android:name=".galary"></activity>
    </application>

</manifest>